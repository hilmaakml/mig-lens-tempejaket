# Normalise line endings so a Windows checkout does not report every file as modified.
# Text files are stored as LF in the repository; Git converts on checkout as needed.
* text=auto eol=lf

# Binary assets must never be converted.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.woff2 binary
*.wasm binary
*.traineddata binary
